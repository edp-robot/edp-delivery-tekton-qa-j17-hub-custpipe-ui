apiVersion: v1
kind: Service
metadata:
  name: {{ include "j17-hub-custpipe-ui.fullname" . }}
  labels:
    {{- include "j17-hub-custpipe-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "j17-hub-custpipe-ui.selectorLabels" . | nindent 4 }}
